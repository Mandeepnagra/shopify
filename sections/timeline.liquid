<div class="timeline">
    <div
        class="container">
        <h3>{{ section.settings.timeline_heading }}</h3>
        <div class="grid grid-rows-2 grid-flow-col md:gap-6">
            {% for block in section.blocks %}
                {% case block.type %}
                  {% when 'timeline_block' %}
                    <div class="text-center pb-6 md:pb-14">
                         <div class="year Evolventa-Bold text-4xl pt-6 md:pt-20 md:text-[7.625rem] md:leading-[65px]" data-target="{{ block.settings.timeline_year }}">{{ block.settings.timeline_year }}</div>
                         <p>{{ block.settings.timeline_description }}</p>
                         <div>
                            {%- if block.settings.timeline_image != blank -%}
                                <img src="{{ block.settings.timeline_image | image_url }}" />
                            {%- endif -%}
                        </div>
                    </div>
                 {% endcase %}
            {% endfor %}
        </div>
    </div>
</div>

{% schema %}
  {
    "name": "timeline",
    "tag": "section",
    "class": "section",
    "settings": [
        {
            "type": "text",
            "id": "timeline_heading",
            "label": "Heading",
            "default": "We are transforming the world."
        }
    ],
    "max_blocks": 4,
    "blocks": [
       {
         "name": "Timeline Block",
         "type": "timeline_block",
         "settings": [
            {
                "type": "image_picker",
                "id": "timeline_image",
                "label": "Timeline Image"
            },{
                "type": "select",
                "id": "timeline_image_position",
                "options": [
                  {
                    "value": "left",
                    "label": "Image on left"
                  }, {
                    "value": "right",
                    "label": "Image on right"
                  }
                ],
                "default": "right",
                "label": "Image Position"
            },{
            "type": "number",
            "id": "timeline_year",
            "label": "Year",
            "default": 2023
           },{
            "type": "textarea",
            "id": "timeline_description",
            "label": "Description",
            "default": "Lorem ipsum dolor sit amet, consectetur adipiscing elit. Integer facilisis vitae enim at scelerisque. Pellentesque gravida pretium interdum."
          }
         ]
       }
    ],    
    "presets": [
        {
          "name": "Timeline"
        }
    ]
  }
{% endschema %}